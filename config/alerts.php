<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 飞书 Webhook 白名单前缀
    |--------------------------------------------------------------------------
    |
    | webhook 地址由用户自由提交、由服务端发起请求，等于把「让服务器去访问任意
    | URL」的能力交给了外部输入。这份前缀白名单是 SSRF 的唯一防线：表单校验时
    | 挡一次，FeishuNotifier 真正发请求前再挡一次。放宽它之前先想清楚后果。
    |
    */
    'webhook_prefixes' => [
        'https://open.feishu.cn/open-apis/bot/v2/hook/',
        'https://open.larksuite.com/open-apis/bot/v2/hook/',
    ],

    /*
    |--------------------------------------------------------------------------
    | 复发提醒间隔（小时）
    |--------------------------------------------------------------------------
    |
    | 余额持续低于阈值时，每隔这么久重复提醒一次，直到充值。余额回到阈值以上会
    | 清空推送记录，下次跌破时重新立即提醒。
    |
    */
    'remind_hours' => (int) env('ALERT_REMIND_HOURS', 24),

    /*
    |--------------------------------------------------------------------------
    | 检查频率
    |--------------------------------------------------------------------------
    |
    | Console\Kernel 里 alerts:check 的 cron 表达式。改完记得 php artisan config:clear。
    |
    */
    'schedule_cron' => env('ALERT_CHECK_CRON', '*/30 * * * *'),

    /*
    |--------------------------------------------------------------------------
    | 默认预警金额（美元）
    |--------------------------------------------------------------------------
    |
    | 用户设置页与管理员全局设置页的表单初值。
    |
    */
    'default_threshold_amount' => (float) env('ALERT_DEFAULT_THRESHOLD', 5),

    /** 调用飞书接口的超时秒数 */
    'http_timeout' => (int) env('ALERT_HTTP_TIMEOUT', 10),

];
